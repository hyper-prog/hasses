This sample docker compose file runs a codkep php web framework and
a hasses daemon container which works together as a SSE based chat page,
where the client browsers can chat to each other with immediate notification
without any database!

To run this:
  $ docker-compose up -d

